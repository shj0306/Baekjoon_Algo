//#include <bits/stdc++.h>
//#define all(x) (x).begin(), (x).end()
//#define fast_io ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
//using namespace std;
//using pii = pair<int,int>;
//
//int n;
//
//int main() {
//    fast_io;
//    cin >> n;
//    vector<double> drink(n);
//    for (int i = 0; i < n; i++) cin >> drink[i];
//
//    sort(all(drink), greater<>());
//
//    double res = drink[0];
//    for (int i = 1; i < n; i++) res += drink[i] / 2;
//    cout << res;
//}