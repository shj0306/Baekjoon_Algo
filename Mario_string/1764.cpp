//#include <bits/stdc++.h>
//#define all(x) (x).begin(), (x).end()
//#define fast_io ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
//using namespace std;
//using pii = pair<int,int>;
//#define f first
//#define s second
//using namespace std;
//
//int main() {
//    fast_io;
//    int N, M; cin >> N >> M;
//    set<string> list, ans;
//    for (int i = 0; i < N + M; i++) {
//        string str; cin >> str;
//        if (i < N) list.insert(str);
//        if (i >= N && list.find(str) != list.end()) ans.insert(str);
//    }
//    cout << ans.size() << '\n';
//    for (auto it = ans.begin(); it != ans.end(); it++) {
//        cout << *it << '\n';
//    }
//}