#include <bits/stdc++.h>
#define all(x) (x).begin(), (x).end()
#define fast_io ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
using namespace std;
using pii = pair<int,int>;
using ll = long long;
int N, M, atom[200], proton[200];

int main() {
    fast_io;
    cin >> N >> M;

    for (int i = 0; i < N; i++) {
        cin >> atom[i];
    }

    for (int i = 0; i < M; i++) {
        cin >> proton[i];
    }




}