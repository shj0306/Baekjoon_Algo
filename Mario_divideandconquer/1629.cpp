#include <bits/stdc++.h>
/*
using namespace std;
typedef long long ll;

ll fpow(int a,int b,int c) {
    if (b == 1) return a;
    else {
        ll x = fpow(a, b/2, c);
        if (b % 2 == 0) {
            return (x*x) % c;
        }else {
            return ((x*x)%c * a) % c;
        }
    }
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a,b,c;
    cin >> a >> b >> c;
    cout << fpow(a%c,b,c) << '\n';
    return 0;
}
*/
